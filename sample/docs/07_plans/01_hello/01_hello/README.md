# Sprint 1-1: hello

> 作成日: 2026-09-10
> ステータス: CLOSED
> 要件 ID: REQ-1

---

## 1. 背景・目的

### 解決する課題
- harness を取り込んだプロジェクトで、PLAN から CLOSED まで全ゲートを実際に通した記録が 1 つも無い
- このスプリントの完了により、取り込み手順の見本として使える完走記録ができる

### 対応する仕様書
| ドメイン | ファイル | セクション |
|---------|---------|----------|
| — | `docs/05_specifications/*.md` | — |

---

## 2. スコープ

**MUST**:
- [x] T-1: `hello()` が `"Hello Claude!"` を返す

**OUT OF SCOPE**:
- 上記以外の機能追加

---

## 3. タスク・進捗

### タスク一覧

| # | タスク | エージェント | 状態 |
|---|--------|------------|------|
| 1 | `hello()` の実装とテスト | backend | ✅ |

状態: ⬜ 未着手 / 🔧 進行中 / ✅ 完了

### BUILD（実装中に埋める）

#### 変更ファイル一覧

| ファイル | 変更種別 | 概要 |
|---------|---------|------|
| `app/src/hello.ts` | 新規 | `hello()` の実装 |
| `app/src/hello.test.ts` | 新規 | `hello()` のテスト |

---

## 4. REVIEW

- 実施日時: 2026-09-10
- 判定: 🟢

| チェック項目 | 結果 | 備考 |
|-------------|------|------|
| 仕様書とスコープの整合 | 🟢 | T-1 は HELLO-1 / N-1.1 に対応 |
| SPEC.md の EARS 要件が TEST.md にカバーされている | 🟢 | N-1.1 → TEST-1-1-1.1 |

### 参照条項の監査（R10。qa が記入する）

SPEC の「0. 参照する共通条項」節の各行について、`bash harness/bin/sprint spec-graph resolve <SPEC.md>` で参照先の条項本文を読み、
扱いの妥当性を判定する。**resolve 出力のどこを読んで判定したかを根拠に書く。**

| 条項 | 扱い | qa 判定 | 根拠（読んだ条項本文の要点） |
|------|------|---------|---------------------------|
| HELLO-1 | 新規 | 🟢 | resolve の本文が「戻り値は "Hello Claude!" である」と定め、TEST-1-1-1.1 がこれを直接検証している |

---

## 5. TEST

- 実施日時: 2026-09-10
- 判定: 🟢

| 項目 | 結果 |
|------|------|
| `bash harness/bin/sprint run lint` | 対象外（app は test のみ） |
| `bash harness/bin/sprint run typecheck` | 対象外（app は test のみ） |
| `bash harness/bin/sprint run build` | 対象外（app は test のみ） |
| `bash harness/bin/sprint run test` | 🟢 pass |
| `bash harness/bin/sprint run e2e` | 対象外（app は test のみ） |

---

## 6. RETRO

### うまくいったこと
- 単一コンポーネント・単一条項の最小構成にしたことで、PLAN から CLOSED までの全ゲートを迷いなく通せた

### 問題点
- 特になし（最小サンプルのため）

### 改善策

- 特になし

### パターン抽出
仕様・実装・プロセスで再利用可能なパターンがあれば記録する。

| パターン | 適用先 | 備考 |
|---------|-------|------|
| — | — | — |

---

## 7. DOCS

実装結果を仕様書にフィードバックする。

| 層 | ファイル | 差分 |
|----|---------|------|
| — | — | 差分なし（実装は仕様どおり） |

### 参照条項の突合（D4。qa が記入する）

`bash harness/bin/sprint spec-graph diff <スプリント開始 commit>` で**内容の変わった条項**を実測し、
SPEC の「0. 参照する共通条項」節の宣言と突合する。宣言と実績が食い違う行は 🔴（例: 「不要」と宣言した条項が変わっている / 「新規」の Test ID が green でない）。

| 条項 | 宣言した扱い | 実績（diff とテストの実測） | 判定 |
|------|------------|---------------------------|------|
| HELLO-1 | 新規 | 変更なし・TEST-1-1-1.1 green | 🟢 |

### 本体コミット（DOCS の最後に実施する）

ベース仕様への反映まで終えたら、**DOCS ステージで実装と文書を commit する。** そのハッシュを §8 に記録する。

- [x] 本体コミット済み（ハッシュは §8 に記録する）

---

## 8. SHIP

- [x] 全タスク ✅
- [x] `bash harness/bin/sprint run all` green
- [x] REVIEW 🔴 = 0
- [x] TEST 🔴 = 0
- [x] RETRO 記入済み
- [x] DOCS 反映済み
- [ ] コミットハッシュ: TBD <!-- DOCS の本体コミット。SHIP のコミットは記録しない -->
