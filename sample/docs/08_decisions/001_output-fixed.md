# 001: 出力を "Hello Claude!" に固定する

## 背景

サンプルの目的はスプリントの流れそのものを示すことであり、実装の複雑さを示すことではない。

## 決定

`hello()` の戻り値を `"Hello Claude!"` に固定する。入力を取らず、設定や環境で変えない。

## 影響

条項 [HELLO-1](../05_specifications/hello.md#HELLO-1) と実装・テストが常に一致する。将来メッセージを可変にする場合は、この決定を上書きする新しい ADR を書く。
