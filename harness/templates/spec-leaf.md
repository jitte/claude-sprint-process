# [モジュール名]: [表題]

> 雛形の使い方: ファイルの先頭に frontmatter を置く。`PFX` をこのファイルの prefix（英大文字 2〜6 字。コーパスで一意）に、`n` を 1 からの連番に置き換える。`[ ]` の中を埋め、この引用ブロックは消す。
> 節の並びは、自層の上位仕様の「構成」節が定める形に合わせる。ここにある節は例である。
>
> ```yaml
> ---
> xref-prefix: PFX
> layer: [任意。上位仕様と同じ値]
> impl: [任意。主実装パス。書くと実在が検証される]
> ---
> ```

## 1. 責務と背景

<a id="PFX-n"></a>
### [PFX-n] [モジュール名]の責務

本ファイルが書き足す差分の範囲は [上位仕様の「下位が書き足すこと」の条項を `[ID](<相対パス>#ID)` で] に従う。

本ファイルは [層の名前] の 1 つである（[上位仕様の「構成」の条項]）。[層に共通する規範の表題] は [上位仕様の条項] に従う。

[固有の責務と境界を書く。上位の規範を再記述しない。他のモジュールとの境界は、相手の名前と何を委ねるかを 1 行ずつ。]

### なぜ必要か

[要件の条項を `[ID](<相対パス>#ID)` で参照して 1 段落。理由は条項にしない。]

---

## 2. [固有の規範の節。ドメインルール、または上位仕様が定める節名]

<a id="PFX-n"></a>
### [PFX-n] [規範の表題]

[EARS で書く。上位の規範に条件を足すときは、上位を参照したうえで追加条件だけを書く。]

---

## 3. [API エンドポイント、または画面の振る舞い。上位仕様が定める節名]

<a id="PFX-n"></a>
### [PFX-n] [エンドポイントまたは操作の表題]

[パス・リクエスト・レスポンス、または操作と実行後の振る舞い。共通の形は参照で従う。]

---

## 4. テスト観点

<a id="PFX-n"></a>
### [PFX-n] [検証すべきケースの表題]

[固有の観点だけ。共通パターンの観点は書かない。]
